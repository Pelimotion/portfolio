{
  "name": "pelimotion-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd projetos-app && npm install && npm run build && cd ../blog-generator && node index.js"
  }
}
